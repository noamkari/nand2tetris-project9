/**
 * The main class of the Brick game.
 */
class Main {

    /** Initializes a Brick game and starts running it. */
    function void main() {
        var BrickGame game;
        do BrickGame.newInstance();
        let game = BrickGame.getInstance();
        do game.run();
        do game.dispose();
        return;
    }
}
